/**
 * @file main.cc 
 * @author Hoo
 * @date 2023-8-14
 * @brief The entry of data base component. It will finish these tasks:
 * open databse, open a unix domain server.
*/
#include <cstdlib>

/**
 * @note OPTIONS NEEDED
 * 
 * 1. Database working directory.(Absolute path)
 * 2. Unix domain server name for binding.(/tmp/name)
 * 3. Number of threads
 * 4. Log MQ name.
*/

int main(int argc, char* argv[])
{
    
}
